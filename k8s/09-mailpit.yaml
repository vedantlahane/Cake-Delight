apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailpit
  namespace: cake-delight
  labels:
    app: mailpit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mailpit
  template:
    metadata:
      labels:
        app: mailpit
    spec:
      containers:
        - name: mailpit
          image: axllent/mailpit:latest
          ports:
            - containerPort: 1025
              name: smtp
            - containerPort: 8025
              name: webui
---
apiVersion: v1
kind: Service
metadata:
  name: mailpit
  namespace: cake-delight
spec:
  type: NodePort
  selector:
    app: mailpit
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: webui
      port: 8025
      targetPort: 8025
      nodePort: 30025
